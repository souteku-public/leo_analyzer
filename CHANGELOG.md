# 変更履歴

測定データの内容・形式に影響する変更を中心にまとめています。
**日時はすべて日本時間(JST)** です(測定CSV内の `timestamp_utc` 列は
UTCなので、突き合わせる際は9時間の差にご注意ください)。
どのバージョンで取ったデータかは、各測定フォルダの `summary.json` の
`tool_version` で確認できます(v0.7 以降)。それ以前のデータは、下表の
「日時」とフォルダ名の日時を突き合わせてください。

**最新版: v0.14**

| 版 | 日時(JST) | 概要 |
|---|---|---|
| v0.14 | 8/10 19:20 | ビューアを**Python不要の単一HTML**でも動作するように |
| v0.13 | 8/10 16:00 | **データビューアを追加**(再生・スカイビュー・雨雲地図) |
| v0.12 | 7/29 21:41 | 測定内容を選択可能に(スループット+RTT / RTTのみ / アンテナのみ) |
| v0.11 | 7/29 20:57 | エラー連発の修正、失敗時の間隔調整、開始前の到達性チェック |
| v0.10 | 7/29 20:29 | スペクトラムのウォーターフォール表示 |
| v0.9 | 7/29 20:17 | CSV肥大化(1.8GB)の修正、`--shrink-csv` 追加 |
| v0.8 | 7/29 19:41 | DLLブロック環境でStarlink取得可能に(純Python gRPC) |
| v0.7 | 7/28 17:48 | Starlink接続診断、変更履歴とバージョン記録の導入 |
| v0.6 | 7/28 10:43 | スペクトラムのCSV分離、`--gzip-csv` |
| v0.5 | 7/27 15:04 | CSV列の自動圧縮、アンテナ比較レポート |
| v0.4 | 7/24 19:26 | **Kymetaのデータ取得が可能に** |
| v0.3 | 7/24 17:37 | グラフレポート `report.html` |
| v0.2 | 7/23 17:52 | **アイドルRTT測定区間を追加** |
| v0.1 | 7/23 16:31 | 初版 |

---

## v0.14 — 2026-08-10(月)19:20 JST

**データビューアがPython無しのPCでも動くようになりました**

- ビューアを `viewer.html` の**単一ファイル**に集約。**ダブルクリックで
  起動**でき、Pythonのインストールは不要(メール添付やUSBで配布可能)
- 同じファイルが `--viewer`(Pythonサーバー)でもそのまま動作。機能差なし
- 単体モードでは、CSVの解析・衛星軌道計算(satellite.js を同梱)・
  降水取得(Open-Meteo)・地図タイル(OpenStreetMap)をすべてブラウザ内で実行
- 衛星の計算結果は Python 版(sgp4)と 0.005° 以内で一致することを確認
- `.csv.gz` はブラウザの展開機能で読み込み(対応ブラウザのみ)

## v0.13 — 2026-08-10(月)16:00 JST

**データ観測用アプリ(データビューア)を追加**

計測アプリとは独立した、取得済みCSVを分析するためのブラウザUIです。
`python -m leo_analyzer --viewer`(Windowsは `viewer.bat`)で起動します。

- 任意のフォルダのCSVをUIから選んで読み込み(`.csv.gz` 対応、
  フォルダ内の複数CSVを時刻で自動結合)
- 1秒〜5分の粒度を指定して時系列を再生(速度 ×1〜×60)
- **スカイビュー**: その時刻に上空にあるStarlink/OneWeb衛星をSGP4で計算し
  極座標表示。アンテナ指向を重ね、**指向に最も近い衛星**を強調、
  直近の指向履歴も描画してハンドオーバーの動きを追えるようにした
- **雨雲地図**: Open-Meteoの降水量をその時刻・その地点のグリッドで地図に
  重ね、走行軌跡とアンテナ位置・指向方位を表示
- **位置情報がないデータでは地図クリックでアンテナ位置を指定可能**
- 衛星TLE(CelesTrak)、降水(Open-Meteo)、地図タイル(OpenStreetMap)は
  ローカルサーバー経由で取得・キャッシュ

## v0.12 — 2026-07-29(水)21:41 JST

**測定内容を選べるようになりました**

- `--measure {full,rtt,none}` を追加(対話モードでも選択可)
  - `full`(既定)= 最大スループット + RTT。従来どおり
  - **`rtt` = RTTのみ。回線に負荷をかけずに測定時間ずっとRTTと
    疎通不能秒数を記録**。走行テストや長時間監視向け
    (12時間でも通信量は約50MB。フル測定は最大1TB規模)
  - `none` = アンテナ情報のみ(従来の `--collect-only` 相当)
- `summary.json` に `measure_mode` を記録
- `--collect-only` は `--measure none` の別名として維持

## v0.11 — 2026-07-29(水)20:57 JST

**アンテナに繋がらないときの挙動を改善**

- **同じ秒に大量の行が記録される不具合を修正**。取得が遅い/失敗すると
  遅れを取り戻そうとして一気に十数回試行し、CSVが同一秒の行で埋まって
  いた。遅れた分は飛ばして次の秒に合わせるよう変更
- **失敗が続くと取得間隔を自動的に広げる**(5回で5秒、60回で15秒、
  300回で30秒間隔。復旧すると元に戻る)
- **エラー文を1行に整形**(gRPCの複数行ダンプで CSV が読みにくく
  なっていた。200文字で打ち切り)
- **測定開始前にStarlinkアンテナへの到達性を確認**し、繋がらない場合は
  警告と対処方法を表示してから測定を続行する

## v0.10 — 2026-07-29(水)20:29 JST

- **スペクトラムのウォーターフォール表示をレポートに追加**。
  `kymeta_*adc*.jsonl.gz` に保存された全スイープを復元し、
  縦=周波数 / 横=時間 / 色=信号強度 のヒートマップとして
  `report.html` に埋め込む(外部ライブラリ不要・PNGを内蔵)
- スペクトラム読み出し用モジュール `leo_analyzer/spectrum.py` を追加
  (base64 float32 のデコード、任意の解析に再利用可能)

## v0.9 — 2026-07-29(水)20:17 JST

**CSV肥大化の残り原因を修正(3時間で1.8GB → 約1MB)**

- **base64形式のスペクトラムを検出できるようにした**。実機の `adc-data` は
  数値配列ではなく **1行あたり約175KBのbase64文字列**(float32の生波形)を
  返しており、配列判定をすり抜けてCSVに毎秒書き込まれていた。
  2048文字を超える文字列も大容量データとして `.jsonl.gz` に退避する
- **測定中の再判定を追加**。探索時にスペクトラムが停止中だと応答が空で
  「通常データ」と判定され、その後巨大化しても切り替わらなかった。
  以降は途中で大きくなった時点で自動的にJSONL退避に切り替える
- base64をfloat32配列としてデコードし、CSVには要約
  (`points`/`min`/`max`/`avg`)を記録
- **既存の巨大CSVを救済する `--shrink-csv` を追加**。巨大セルを
  `_bulk.jsonl.gz` に分離し、軽量な `_slim.csv` を出力する

## v0.8 — 2026-07-29(水)19:41〜19:46 JST

**Starlinkのデータが取れなかった環境への対応**

- **純Python版のgRPCクライアントを追加**。会社支給PCなどでWindowsの
  アプリケーション制御ポリシーが grpcio のネイティブDLL(`cygrpc.pyd`)を
  ブロックしている環境でも、DLLを使わずに(h2ライブラリで)アンテナ情報を
  取得できるようにした。既定は自動切替(`--starlink-transport`)
- protobufも純Python実装を使うよう指定(同様のDLLブロック対策)
- `--starlink-probe` の診断に「grpcioが使えるか」の判定を追加
- 実機が返す `NaN`(未計測の統計値)でグラフが白紙になる不具合を修正。
  レポートの優先表示項目を実機のフィールド名に合わせた

## v0.7 — 2026-07-28(火)17:48〜18:03 JST

- **Starlink接続の診断コマンド `--starlink-probe` を追加**。
  プロキシ環境変数 → TCP接続 → gRPCリフレクション → `get_status` の
  4段階を切り分け、失敗箇所と対処を表示
- **gRPC接続でプロキシ設定を無効化**(`grpc.enable_http_proxy=0`)。
  社内プロキシが設定されたPCでは、LAN宛の `192.168.100.1` への接続まで
  プロキシ経由になり失敗していた可能性がある(**Starlinkのデータが
  取れなかった場合の有力な原因**)
- 長時間測定でgRPC接続が固まらないようキープアライブを追加
- ステータスが空で返った場合をエラーとして記録するよう変更
- **変更履歴(このファイル)を追加**し、`summary.json` に
  `tool_version` を記録するようにした

## v0.6 — 2026-07-28(火)10:43〜12:46 JST

**データ形式に大きな変更あり**

- **スペクトラムがCSVから分離**(重要な修正)。実機の `adc-data` が
  `[[周波数, 電力], ...]` の入れ子形式のため大容量判定から漏れ、
  CSVの1セルに毎秒埋め込まれていた(13時間で8GB)。以降は
  `kymeta_<名前>.jsonl.gz` に退避し、CSVには要約列
  (`points` / `min` / `max` / `avg`)のみ (`b88647c`)
- 形式によらず4KB超の応答は大容量扱いに変更。CSVの1セルは400文字上限
- `--gzip-csv` を追加(CSVを `.csv.gz` で保存、約1/10)
- `--kymeta-probe --kymeta-full` で全項目表示、`kymeta_sample.json` に
  1サンプル分の完全データを保存 (`cca0b74`)
- `summary.json` に `tool_version` を記録開始

> **v0.6 より前の `kymeta_status.csv` は、スペクトラムが埋め込まれて
> 巨大化している場合があります。**該当列を削除すれば通常のデータとして
> 利用できます。

## v0.5 — 2026-07-27(月)15:04〜15:17 JST

**CSVの列構成が変わります**

- **CSV自動圧縮**: 開始から約90秒の観測で、値が返らない項目は列ごと省略、
  ずっと同じ値の項目は `<名前>_static.json` に退避(実測127列→十数列)。
  後からの値変化は `notes` 列、部分失敗は `error` 列に記録 (`0c64d3d`)
  - 全項目を残すには `--keep-all-columns`
- スペクトラム取得間隔のオプション `--kymeta-array-interval` 追加
- 空きディスク1GB以下・1測定2GB超で大容量データの記録を自動停止
- **アンテナ比較レポート** `--compare` を追加。Starlink/Kymetaの共通項目
  (スループット、RTT、ロス率、SNR/SINR、仰角・方位角、遮蔽)を同じ
  時刻軸で重ねた `compare.html` を生成 (`7b356c5`)
- `summary.json` にファイルサイズ内訳を追加

## v0.4 — 2026-07-24(金)19:26〜20:39 JST

**Kymetaのデータがここで初めて取得可能に**

- 実機のAPIベース `/v1/`(`/v1/internal/status` など)に対応。
  `x-requested-from: gui` ヘッダー送信、Cookie認証方式を追加 (`9c4ffe4`)
- 未認証時に404を返すファームに対応(見つからなければログインを試行)
  (`0b19339`)
- 実機で確認したエンドポイント名を優先探索。スペクトラムは既定5秒間隔に
  (`c54250e`)
- セッション切れ時の自動再ログイン (`5e68047`)
- 探索の診断ログを追加 (`13c1c38`)
- 依存ライブラリの自動インストール案内 (`2a9ec76`)

## v0.3 — 2026-07-24(金)17:37〜18:35 JST

- **グラフレポート `report.html` を自動生成**(1ファイル完結・オフライン)
  (`3a52117`)
- Kymetaのplots/spectrumデータ取得に対応(配列データのJSONL退避、
  WebSocketストリーム記録) (`c3b8f4c`)
- WebGUIアプリ(JS)を解析してAPIパスを抽出する探索方式 (`1e10a2e`)

## v0.2 — 2026-07-23(木)17:52〜19:13 JST

**RTTの測定方法が変わります**

- **アイドルRTT測定区間を追加**。「無負荷 → 下り → 無負荷 → 上り」の順に
  進み、`throughput.csv` に `phase=baseline` の行が入る (`02304b7`)
- アイドル測定を既定5分に延長。疎通不能の秒を統計から除外し
  `unreachable_s` として集計 (`15faf0f`)
- `summary.json` に `latency_ms`(idle / download_loaded / upload_loaded)
  を追加
- 測定時間を `10m` / `1h` 形式で指定可能に。既定を方向ごと5分に延長
- 引数なし起動で対話モード、`measure.bat` を追加 (`5b2095b`)

> **v0.2 より前のデータには `baseline` 行がなく、`latency_ms` はすべて
> 負荷中の値です。**アイドルRTTとの比較はできません。

## v0.1 — 2026-07-23(木)16:31〜16:48 JST

- 初版。Cloudflare対向の並列HTTPによる1秒解像度スループット測定、
  Starlink(gRPC)/ Kymeta のテレメトリ同時記録 (`6f51285`)
- Kymetaのエンドポイント自動探索(この時点では実機では未到達) (`d3501e2`)

---

## データを比較する際の注意

| 比較したい内容 | 必要なバージョン |
|---|---|
| スループット(下り/上り) | 全バージョンで比較可能 |
| アイドルRTT / 疎通不能秒数 | v0.2 以降 |
| Kymetaのアンテナ情報 | v0.4 以降 |
| **Starlinkのアンテナ情報**(DLLブロック環境) | **v0.8 以降** |
| Kymetaのスペクトラム(正常な容量で) | v0.9 以降 |
| CSVの列構成 | v0.5 で変更(それ以前は全列出力) |
| 測定モード(`measure_mode`)の記録 | v0.12 以降 |

## 既存データの見分け方(v0.7 より前)

`summary.json` に `tool_version` が無いデータは、フォルダ名の日時
(UTC)を上表と突き合わせてください。中身からも判別できます:

- `throughput.csv` に `baseline` の行がある → v0.2 以降
- `kymeta_status_static.json` がある → v0.5 以降
- `kymeta_status.csv` が異常に大きい → v0.9 未満
  (`--shrink-csv` で軽量化できます)
